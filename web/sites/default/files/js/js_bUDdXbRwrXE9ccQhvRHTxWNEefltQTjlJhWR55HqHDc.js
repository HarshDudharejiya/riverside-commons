/* @license GPL-2.0-or-later https://www.drupal.org/licensing/faq */
(function($,Drupal,once){Drupal.behaviors.betterExposedFiltersAutoSubmit={attach:function(context,settings){let selectors='';if(drupalSettings.better_exposed_filters&&drupalSettings.better_exposed_filters.auto_submit_sort_only)selectors='form[data-bef-auto-submit-sort-only] .form-item-sort-order';else selectors='form[data-bef-auto-submit-full-form], [data-bef-auto-submit-full-form] form, [data-bef-auto-submit]';$(selectors,context).addBack(selectors).find('input:text:not(.hasDatepicker), textarea, select, input:radio, input:checkbox').each(function(){const $el=$(this);const $tagName=$el[0].tagName;if($tagName==='SELECT')setFocus($el);else if(['radio','checkbox'].includes($el.attr('type')))setFocus($el);else setFocus($el);});function setFocus($el){const observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(entry.isIntersecting){const lastTriggeredSelector=settings.bef_autosubmit_triggered_selector;const lastCursorPosition=settings.bef_autosubmit_cursor_position;const currentSelector=$el.attr('data-drupal-selector');if(lastTriggeredSelector&&currentSelector){const lastTriggeredBase=lastTriggeredSelector.replace(/-[a-z0-9]{11}$/,'');const currentBase=currentSelector.replace(/-[a-z0-9]{11}$/,'');if(currentBase===lastTriggeredBase||currentSelector.startsWith(lastTriggeredBase+'-')){$el.focus();if($el.is(':text, textarea')&&typeof lastCursorPosition==='number')try{$el[0].setSelectionRange(lastCursorPosition,lastCursorPosition);}catch(e){}}}observer.disconnect();}});});observer.observe($el.get(0));}$(selectors,context).addBack(selectors).each(function(i,e){const $e=$(e);let $needsAutoSubmit=$e;let autoSubmitDelay=500;if(e.hasAttribute('data-bef-auto-submit-delay'))autoSubmitDelay=$e.data('bef-auto-submit-delay');else{if(!e.hasAttribute('data-bef-auto-submit-full-form')){const $container=$e.closest('[data-bef-auto-submit-full-form]');if($container.length&&$container.get(0).hasAttribute('data-bef-auto-submit-delay')){$needsAutoSubmit=$container;autoSubmitDelay=$container.data('bef-auto-submit-delay');}}}autoSubmitDelay=autoSubmitDelay||500;let dateInputChangeDebounce=Drupal.debounce(triggerSubmit,autoSubmitDelay,false);$(once('bef-auto-submit',$needsAutoSubmit)).on('change',function(e){return e.target.type==='date'?dateInputChangeDebounce(e):triggerSubmit(e);}).on('keyup',Drupal.debounce(triggerSubmit,autoSubmitDelay,false));});function triggerSubmit(e){const ignoredKeyCodes=[16,17,18,20,33,34,35,36,37,38,39,40,9,13,27,32];const $target=$(e.target);const $form=e.target.form?$(e.target.form):$target.closest('form');const $submit=$form.find('[data-bef-auto-submit-click]').first();const mediaQuery=$target.closest('form').data('bef-auto-submit-media-query');if(mediaQuery&&!matchMedia(mediaQuery).matches)return true;if($target.is('[data-bef-auto-submit-exclude], :submit, .select2-search__field, .chosen-search-input'))return true;let textfieldMinimumLength=$target.closest('form').data('bef-auto-submit-minimum-length')||1;let inputTextLength=$target.val().length;let textfieldHasMinimumLength=($target.is(':text:not(.hasDatepicker), textarea')&&inputTextLength>=textfieldMinimumLength)||$target.is('[type="checkbox"], [type="radio"]');if((textfieldHasMinimumLength||inputTextLength===0)&&$.inArray(e.keyCode,ignoredKeyCodes)===-1&&!e.altKey&&!e.ctrlKey&&!e.shiftKey)$submit.click();else{if(e.type==='change'){let target=$target.is(':text:not(.hasDatepicker), textarea');if(target&&textfieldHasMinimumLength)$submit.click();else{if(!target)$submit.click();}}}settings.bef_autosubmit_triggered_selector=$target.attr('data-drupal-selector');if($target.is(':text, textarea'))try{settings.bef_autosubmit_cursor_position=$target[0].selectionStart;}catch(e){settings.bef_autosubmit_cursor_position=$target.val().length;}}}};}(jQuery,Drupal,once));;
(function($,Drupal,once){Drupal.behaviors.betterExposedFilters={attach:function(context){$('.bef-tree input[type=checkbox], .bef-checkboxes input[type=checkbox]').change(function(){_bef_highlight(this,context);}).filter(':checked').closest('.form-item',context).addClass('highlight');}};function _bef_highlight(elem,context){$elem=$(elem,context);$elem.attr('checked')?$elem.closest('.form-item',context).addClass('highlight'):$elem.closest('.form-item',context).removeClass('highlight');}Drupal.behaviors.autosubmitExcludeTextfield={attach:function(context,settings){if(!settings.better_exposed_filters?.autosubmit_exclude_textfield)return;$(once('autosubmit-exclude-textfield','.bef-exposed-form',context)).each(function(){$(this).find('*[type="text"]').attr('data-bef-auto-submit-exclude','');});}};Drupal.behaviors.befSingleCheckboxFix={attach:function(context,settings){once('befSingleCheckboxFix','form.views-exposed-form',context).forEach((form)=>{const $form=$(form);const $checkboxes=$form.find('input[type="checkbox"].single-checkbox');$form.on('submit',function(){$checkboxes.each(function(){const $checkbox=$(this);const name=$checkbox.attr('name');$form.find('input[type="hidden"][name="'+name+'"]').remove();if(!$checkbox.is(':checked'))$('<input>').attr({type:'hidden',name,value:0}).appendTo($form);});});});}};})(jQuery,Drupal,once);;
