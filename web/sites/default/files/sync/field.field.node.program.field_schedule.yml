langcode: en
status: true
dependencies:
  config: [field.storage.node.field_schedule, node.type.program]
id: node.program.field_schedule
field_name: field_schedule
entity_type: node
bundle: program
label: Schedule
description: 'Human-readable recurring schedule override, e.g. "Tues & Thurs, 6-8pm". When empty, the formatted Schedule (dates) field is shown instead.'
required: false
translatable: false
default_value: []
default_value_callback: ''
settings: { max_length: 255 }
field_type: string
